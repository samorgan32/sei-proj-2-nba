import React from 'react';

const About = () => {
    return (
        <div>
            <p>Find out which NBA player's are actually the best by comparing the stats directly.  Search for a player on the left to start checking out stats!</p>


            <p>All statistics are for the 2020 NBA season, and are sourced from the balldontlie API.</p>
        </div>
    );
};

export default About;